PROJECT(libsquash C)
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

FIND_PACKAGE(ZLIB REQUIRED)
INCLUDE_DIRECTORIES(${ZLIB_INCLUDE_DIRS})
LINK_LIBRARIES(${ZLIB_LIBRARIES})

SET(SRC_H cache.h)
SET(SRC_SQUASH cache.c)
ADD_LIBRARY(squash ${SRC_H} ${SRC_SQUASH})
